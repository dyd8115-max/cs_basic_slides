// API keys — copy this file to config.js and fill in your keys
// config.js is gitignored and will NOT be committed

// 1. Naver Maps JS SDK: console.ncloud.com → AI·NAVER API → Web Dynamic Map
window.NAVER_CLIENT_ID   = 'YOUR_MAPS_CLIENT_ID';

// 2. NCP IAM credentials: console.ncloud.com → 마이페이지 → 인증키 관리
//    → "새 API 인증키 생성"
//    Geocoding + Directions 15 API 에 사용됩니다
window.NCP_API_KEY_ID    = 'YOUR_ACCESS_KEY_ID';   // Access Key ID
window.NCP_API_SECRET    = 'YOUR_SECRET_KEY';       // Secret Key (절대 공개 금지)

// 3. Anthropic (Claude): console.anthropic.com → API Keys
window.ANTHROPIC_API_KEY = 'sk-ant-...';
